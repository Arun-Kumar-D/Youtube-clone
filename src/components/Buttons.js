import React from 'react'

const Buttons = ({name}) => {
  return (
    <div className=''>
        <button className='px-5 py-1 m-2 rounded-lg bg-gray-200'>{name}</button>
    </div>
  )
}

export default Buttons